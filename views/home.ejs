<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quiz Website</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #e0e7ff, #f0f9ff);
      font-family: 'Poppins', sans-serif;
      height: 100vh;
      margin: 0;
      display: flex;
      flex-direction: column;
    }

    .navbar-brand {
      font-weight: 700;
      color: #4338ca !important;
    }

    /* Right Sidebar */
    .offcanvas-end {
      background-color: #fff;
      width: 230px !important;
      border-left: 2px solid #e2e8f0;
    }

    .offcanvas a {
      color: #374151;
      font-weight: 500;
      display: flex;
      align-items: center;
      gap: 10px;
      text-decoration: none;
      padding: 10px 20px;
      border-radius: 8px;
      margin: 6px 10px;
      transition: all 0.2s ease-in-out;
    }

    .offcanvas a:hover {
      background-color: #eef2ff;
      color: #4338ca;
    }

    /* Center Content */
    .main-content {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 20px;
    }

    .btn-login {
      background-color: #4338ca;
      color: #fff;
    }

    .btn-login:hover {
      background-color: #3730a3;
      color: #fff;
    }
  </style>
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="#"><i class="bi bi-puzzle-fill me-2"></i>QuizSite</a>
      
      <div class="d-flex align-items-center gap-2">
        <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#loginModal">
          <i class="bi bi-box-arrow-in-right me-1"></i>Login
        </button>
        <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasSidebar">
          <span class="navbar-toggler-icon"></span>
        </button>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="main-content">
    <h1 class="display-5 fw-bold text-primary mb-3">Welcome to the Quiz Site 🎓</h1>
    <p class="lead text-muted mb-4">Challenge your mind, compete with friends, and learn something new every day!</p>
    <button class="btn btn-lg btn-login" data-bs-toggle="modal" data-bs-target="#loginModal">
      <i class="bi bi-box-arrow-in-right me-2"></i>Login to Start Quiz
    </button>
  </main>

  <!-- Right Sidebar (Offcanvas) -->
  <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasSidebar" aria-labelledby="offcanvasSidebarLabel">
    <div class="offcanvas-header border-bottom">
      <h5 class="offcanvas-title fw-bold text-primary" id="offcanvasSidebarLabel">
        <i class="bi bi-list"></i> Menu
      </h5>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas"></button>
    </div>
    <div class="offcanvas-body">
      <a href="#"><i class="bi bi-house-door"></i> Login</a>
      <a href="#"><i class="bi bi-info-circle"></i> About Us</a>
      <a href="#"><i class="bi bi-telephone"></i> Contact Us</a>
      <a href="#"><i class="bi bi-lightbulb"></i> Quiz</a>
    </div>
  </div>

  <!-- Login Modal -->
  <div class="modal fade" id="loginModal" tabindex="-1" aria-labelledby="loginModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content rounded-4 shadow">
        <div class="modal-header border-0">
          <h5 class="modal-title text-primary fw-bold" id="loginModalLabel">Login</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body text-center">
          <p class="text-muted mb-3">Select your role to continue:</p>
          <div class="d-grid gap-3">
            <form method="get" action="/student_login">
              <button class="btn btn-primary py-2">
                <i class="bi bi-person-fill me-2"></i>Login as Student
              </button>
            </form>
            
            <form method="get" action="/teacher_login">
              <button class="btn btn-success py-2">
                <i class="bi bi-mortarboard-fill me-2"></i>Login as Teacher
              </button>
            </form>

          </div>
        </div>
        <div class="modal-footer border-0 text-center">
          <button type="button" class="btn btn-outline-secondary w-100" data-bs-dismiss="modal">
            <i class="bi bi-arrow-left-circle me-2"></i>Return to Quiz Site
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
